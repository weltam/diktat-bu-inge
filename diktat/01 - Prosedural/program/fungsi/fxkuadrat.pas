program fxkuadrat;
(* File : fxkuadrat.pas*)
(* Description :*)
	function fx_kuadrat(x : integer) : integer;
	begin
		fx_kuadrat := (x * x + 3 * x - 5);
	end;
	
	function FXY(x, y : real) : real;
	begin
		x : integer
	end;
begin
	writeln(fx_kuadrat(5));
end.